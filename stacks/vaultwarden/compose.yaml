services:
  vaultwarden:
    image: docker.io/vaultwarden/server:1.35.4
    restart: unless-stopped
    environment:
      WEBSOCKET_ENABLED: "true"
      SIGNUPS_ALLOWED: ${VAULTWARDEN_SIGNUPS_ALLOWED:-false}
      DOMAIN: https://${VAULTWARDEN_HOST:-vaultwarden.fewa.app}
      URL: https://${VAULTWARDEN_HOST:-vaultwarden.fewa.app}
      # SSO / authentik
      SSO_ENABLED: "true"
      SSO_PKCE: "true"
      SSO_AUTHORITY: ${VAULTWARDEN_SSO_AUTHORITY:-}
      SSO_CLIENT_ID: ${VAULTWARDEN_SSO_CLIENT_ID:-}
      SSO_CLIENT_SECRET: ${VAULTWARDEN_SSO_CLIENT_SECRET:-}
      SSO_SCOPES: "email profile offline_access"
      SSO_ALLOW_UNKNOWN_EMAIL_VERIFICATION: "false"
      SSO_CLIENT_CACHE_EXPIRATION: "0"
      SSO_ONLY: ${VAULTWARDEN_SSO_ONLY:-false}
      SSO_SIGNUPS_MATCH_EMAIL: "true"
    volumes:
      - vaultwarden_data:/data
    networks:
      - shared
    labels:
      traefik.enable: "true"
      traefik.docker.network: ${SHARED_DOCKER_NETWORK:-internal-network}
      traefik.http.routers.vaultwarden.rule: Host(`${VAULTWARDEN_HOST:-vaultwarden.fewa.app}`)
      traefik.http.routers.vaultwarden.entrypoints: websecure
      traefik.http.routers.vaultwarden.tls: "true"
      traefik.http.routers.vaultwarden.tls.certresolver: letsencrypt
      traefik.http.services.vaultwarden.loadbalancer.server.port: "80"

networks:
  shared:
    name: ${SHARED_DOCKER_NETWORK:-internal-network}
    external: true

volumes:
  vaultwarden_data:
