---
# Managed by Ansible — do not edit by hand
# NocoDB — no-code database platform

networks:
  infra_net:
    external: true

services:
  nocodb:
    image: "{{ host.data.nocodb_image }}"
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:{{ host.data.nocodb_port }}:8080"
    volumes:
      - "{{ host.data.nocodb_dir }}/data:/usr/app/data"
    networks:
      - infra_net
    mem_limit: 1g
