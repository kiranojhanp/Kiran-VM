# Git worktrees
.worktrees/

# Secrets — never commit
provision/secrets.yml
.secrets.env

# Provisioning local env
provision/.env
provision/.venv

# Inventory — machine-specific, never commit (use hosts.ini.example as template)
provision/inventory/hosts.ini

# Pulumi infra — build output and dependencies
infra/node_modules/
infra/bin/

# Ruff lint cache
**/.ruff_cache/
stacks/garage/garage-webui.env
