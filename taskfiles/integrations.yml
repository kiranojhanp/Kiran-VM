version: '3'

tasks:
  # Repo webhook triggers are now handled by GitHub Actions (.github/workflows/repo-deploy.yml)
  # No local task needed - GitHub calls Komodo's repo webhook directly on push to main
