[fewaapp]
# Written by Pulumi after instance creation — see infra/provisioner.ts
# For standalone use, replace <IP> with your server's public IP
<IP> ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_ed25519 ansible_ssh_port=22
