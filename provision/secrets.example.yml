# Copy/encrypt this file via: `task secrets:init`
# Then edit values via: `task secrets:edit`

deploy_password: ""
common_deploy_ssh_public_key: ""

shared_postgres_password: ""
redis_password: ""

ferretdb_postgres_password: ""
komodo_password: ""
komodo_passkey: ""
komodo_webhook_secret: ""
komodo_jwt_secret: ""
komodo_init_admin_username: "admin"

cloudflare_api_token: ""

# backup (required when backup_enabled=true)
# Use a local path to start simple, e.g. /var/backups/restic-repo
# Later you can switch to rclone backend, e.g. rclone:gdrive:kiran-vm-backups
backup_restic_repository: ""
backup_restic_password: ""
backup_shared_postgres_password: ""

# Optional for rclone backends: manage /root/.config/rclone/rclone.conf via Ansible Vault
# Paste full rclone.conf content here as a multiline value.
# If empty, role expects the file to already exist on host.
backup_rclone_config: |
  

# Optional: S3-compatible repository credentials
backup_aws_access_key_id: ""
backup_aws_secret_access_key: ""
backup_aws_default_region: ""
