---
# common_deploy_ssh_public_key: the SSH public key content to add to authorized_keys.
# Set this in secrets.yml or pass via --extra-vars.
# Example: common_deploy_ssh_public_key: "ssh-ed25519 AAAA... user@host"
common_deploy_ssh_public_key: ""
